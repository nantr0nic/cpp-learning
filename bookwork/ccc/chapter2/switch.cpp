// switch

switch (condition) {
    case (case-a): {
        // If 'condition' matches 'case-a' then: Handle case 'a' here
    } break;
    case (case-b): {
        // If 'condition' matches 'case-b' then: Handle case 'b' here
    } break;
    // Handle other conditions as desired
    default: {
        // If 'condition' matches none of the cases then: Handle the default case here
    }
}
